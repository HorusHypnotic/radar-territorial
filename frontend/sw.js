const CACHE_NAME = "opera-territorial-v5";
const CORE = ["./", "./index.html", "./manifest.webmanifest", "./icons/icon-192.png", "./icons/icon-512.png", "./css/main.css", "./css/theme-dark.css", "./js/main.js", "./js/mapa.js", "./js/kpis.js", "./js/graficos.js", "./js/tabela.js", "./js/upload.js", "./js/apmo.js", "./js/data/sample.js", "./vendor/leaflet/leaflet.css", "./vendor/leaflet/leaflet.js", "../data/output/dashboard_data.json", "../data/output/zonas_poligonos.geojson", "../data/output/livro_razao.json"];

self.addEventListener("install", (event) => { event.waitUntil(caches.open(CACHE_NAME).then((cache) => cache.addAll(CORE)).then(() => self.skipWaiting())); });
self.addEventListener("activate", (event) => { event.waitUntil(caches.keys().then((keys) => Promise.all(keys.filter((key) => key !== CACHE_NAME).map((key) => caches.delete(key)))).then(() => self.clients.claim())); });
self.addEventListener("fetch", (event) => { if (event.request.method !== "GET" || new URL(event.request.url).origin !== self.location.origin) return; const navigation = event.request.mode === "navigate"; event.respondWith(fetch(event.request).then((response) => { if (response.ok) { const copy = response.clone(); event.waitUntil(caches.open(CACHE_NAME).then((cache) => cache.put(event.request, copy))); } return response; }).catch(async () => (await caches.match(event.request)) || (navigation ? caches.match("./index.html") : Response.error()))); });
